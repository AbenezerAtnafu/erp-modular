@model IEnumerable<ERP.Models.HRMS.Language.Language>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ability_to_listen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ability_to_Speak)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ability_to_Read)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ability_to_write)
            </th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ability_to_listen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ability_to_Speak)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ability_to_Read)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ability_to_write)
            </td>
          
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
